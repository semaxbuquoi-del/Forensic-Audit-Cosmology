# AUTOMATED NOZZLE DETECTION
if len(clean_snaps) > 0:
    # 1. Calculate the spatial centroid of the metric failures
    nozzle_ra = clean_snaps['ra'].median()
    nozzle_dec = clean_snaps['dec'].median()
    nozzle = np.array([nozzle_ra, nozzle_dec])
    
    print(f"--- AUTO-DETECTION COMPLETE ---")
    print(f"Nozzle found at: RA {nozzle_ra:.2f}, Dec {nozzle_dec:.2f}")
    
    # 2. Compare to the "Core" (The average of the healthy population)
    core = np.array([df['ra'].median(), df['dec'].median()])
    
    # 3. Calculate the "Siphonic Offset" (Distance from average to nozzle)
    offset = np.linalg.norm(core - nozzle)
    print(f"Siphonic Offset Magnitude: {offset:.4f} degrees")
else:
    print("Nozzle detection failed: Zero 'Clean Snaps' found in this sector.")
